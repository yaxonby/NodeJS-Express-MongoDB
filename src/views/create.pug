include layout/_head.pug
body
  h2 Blog
  form(method="POST")
    p
      b Title:
    input(type="text" name="title")
    p 
      b Author:
    input(type="text" name="author")
    p
      b  Post:
    textarea(type="text" name="body" cols="30" rows="3")
    br
    button add Post
  ul
    each val in [{title: "Двигатель", author: "Юра", body: "выгнало масло из двигателя"}]
      li=' ' + val.title + ' ' +val.author + ' ' + val.body+' '+ val.created_at
include layout/_footer.pug
      